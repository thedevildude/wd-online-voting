<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="/css/styles.css">
  <title>
    <%=title%>
  </title>
</head>

<body>
  <div class="flex justify-between items-center bg-blue-500">
    <p class="text-3xl ml-5 font-medium text-white"><%=title%></p>
    <div class="flex items-center">
      <a href="/login" class="text-2xl my-4 mr-5 text-white bg-blue-900 rounded-xl p-2 w-56 text-center">Admin Login</a>
    </div>
  </div>
  <% if(messages && messages.error && messages.error.length > 0)  { %>
    <% for(var i = 0; i < messages.error.length; i++) { %>
      <p class="bg-red-400 m-5 rounded px-4 py-2"><%= messages.error[i] %></p>
    <% } %>
  <% } %>
  <% if(messages && messages.success && messages.success.length > 0)  { %>
    <% for(var i = 0; i < messages.success.length; i++) { %>
      <p class="bg-green-400 m-5 rounded px-4 py-2"><%= messages.success[i] %></p>
    <% } %>
  <% } %>
  <div class="flex items-center justify-start m-5">
    <a href="./" class="text-xl text-white bg-blue-800 rounded-xl p-2 mr-5 w-40 text-center">Back</a>
  </div>
  <%if(electionEndedLink){%>
  <form class="flex flex-col m-5" action="../<%=electionId%>/voter-login" method="post">
  <%} else{%>
  <form class="flex flex-col m-5" action="./<%=electionId%>/voter-login" method="post">
  <%}%>
    <input type="hidden" name="_csrf" value="<%=csrfToken%>">
    <label class="text-xl text-gray-800 my-2" for="voter_id">Voter ID</label>
    <input class="border border-green-600 rounded-xl p-5 my-2 text-3xl" placeholder="voterid2345" type="text"
      name="voter_id" required>
    <label class="text-xl text-gray-800 my-2" for="password">Password</label>
    <input class="border border-green-600 rounded-xl p-5 my-2 text-3xl" placeholder="*******" type="password"
      name="password">
    <button class="text-2xl text-white bg-green-600 rounded-xl p-2 text-center" type="submit">Start Voting</button>
  </form>
</body>
</html>