<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="/css/styles.css">
  <title>
    <%=title%>
  </title>
</head>

<body>
  <%- include("header", {text: "Create a new election", signOutVis: true })%>
  <% if(messages && messages.error && messages.error.length > 0)  { %>
    <% for(var i = 0; i < messages.error.length; i++) { %>
      <p class="bg-red-400 m-5 rounded px-4 py-2"><%= messages.error[i] %></p>
    <% } %>
  <% } %>
  <div class="flex items-center justify-start m-5">
    <a href="../" class="text-xl text-white bg-blue-800 rounded-xl p-2 mr-5 w-40 text-center">Back</a>
  </div>
  <form class="flex flex-col m-5" action="/home/election/new" method="post">
    <input type="hidden" name="_csrf" value="<%=csrfToken%>">
    <label class="text-xl text-gray-800" for="name">What's the name of your election?</label>
    <input class="border border-green-600 rounded-xl p-5 my-5 text-3xl" placeholder="minimum 3 characters" type="text"
      name="name">
    <button class="text-2xl text-white bg-green-600 rounded-xl p-2 text-center" type="submit">Create</button>
  </form>
</body>
</html>